cmake_minimum_required(VERSION 3.6)

project(GAME)

# armeabi-v7a requires cpufeatures library
# include(AndroidNdkModules)
# android_ndk_import_module_cpufeatures()

# Your game and its CMakeLists.txt are in the project root directory

set(MODULE_NAME main)
set(BUILD_SHARED_LIBRARY ON CACHE BOOL "Build as a shared library" FORCE)
add_subdirectory(../../../ ../../../build_android)

